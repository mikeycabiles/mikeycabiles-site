# Mikey Cabiles — Personal Brand Site

Static HTML/CSS/JS prototype for mikeycabiles.com.

## Local

Open `index.html` in a browser.

## Deploy

Designed to be hosted as a static site (GitHub Pages or similar). Updates ship when changes are pushed to the `main` branch.

